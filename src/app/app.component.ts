import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <div>
      <app-game-container></app-game-container>
    </div>
  `,
})
export class AppComponent {
}
